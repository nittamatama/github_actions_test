name: Tests Workflow
on: push

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
    - name: Set state
      run: |
        echo "MY_STATE=some_value" >> $GITHUB_STATE

    - name: Get state
      run: |
        echo "The state is ${{ state.MY_STATE }}"